# Dev Machine Assessment — Phase 1

**Project:** provider-github-copilot
**Phase:** Phase 1 — SDK Integration + Real Implementation
**Date:** 2026-03-09
**Overall Verdict:** PROCEED WITH CAUTION

## Gate Scores

| Gate | Score | Verdict |
|------|-------|---------|
| 1. Decomposability | 72% | CAUTION |
| 2. Verifiable Correctness | 80% | PASS |
| 3. Sufficient Architecture | 78% | PASS |
| 4. Functioning Toolchain | 60% | CAUTION |
| 5. Spec Quality | 70% | CAUTION |

## Per-Gate Analysis

### Gate 1: Decomposability (72%)

**Evidence:**
- 7 concrete features identified (F-010 through F-016)
- Clear module boundaries (all in `sdk_adapter/`)
- Reference implementation exists (620+ lines of `sdk_driver.py` to guide design)

**Risk:**
- Features are MORE interconnected than Phase 0
- Loop Controller ↔ Tool Capture ↔ Event Router form a tight unit
- Not as decomposable as Phase 0's independent modules

**Verdict:** CAUTION — Features exist but have tighter coupling than Phase 0.

---

### Gate 2: Verifiable Correctness (80%)

**Evidence:**
- Test framework proven: `uv run pytest tests/ -v` works
- Build commands work: `ruff check`, `pyright`
- Type checking enforced (pyright strict mode)
- Fast cycle (<30 seconds)

**Risk:**
- E2E tests require `GITHUB_TOKEN` (not all environments have this)
- Real SDK calls add latency and network dependency
- Tests can skip gracefully without token

**Verdict:** PASS — Toolchain proven in Phase 0.

---

### Gate 3: Sufficient Architecture (78%)

**Evidence:**
- GOLDEN_VISION_V2.md is comprehensive (925 lines, validated by 10+ specialist agents)
- Reference implementation exists (`sdk_driver.py` = 620 lines, `client.py` = 1642 lines)
- SDK types documented (`copilot/types.py` = 1104 lines, `session_events.py` = 2344 lines)
- SETUP-GUIDE.md captures Phase 0 lessons (332 lines)

**Gaps:**
- Draft specs (F-010-F-016) have errors (wrong env var name)
- Draft specs weren't validated through `/brainstorm`

**Verdict:** PASS — Architecture is solid, but draft specs need correction.

---

### Gate 4: Functioning Toolchain (60%)

**Evidence:**
- `github-copilot-sdk>=0.1.32,<0.2.0` already in pyproject.toml
- `amplifier-core` in dev dependencies
- Entry point configured
- Docker and recipe infrastructure working

**Blocker:**
- **7 pyright errors exist from Phase 0** — build is NOT clean
- Errors: unused imports, null safety violations, unknown types

**Verdict:** CAUTION — Toolchain exists but build has errors that must be fixed first.

---

### Gate 5: Spec Quality (70%)

**Evidence:**
- Feature spec template exists
- Reference implementation provides detailed guidance
- SDK types are well documented (2344 lines of generated types)

**Issues:**
- Draft specs use `COPILOT_AGENT_TOKEN` but should use `GITHUB_TOKEN`
- Draft specs weren't validated through `/brainstorm`
- Reference README documents auth: `COPILOT_GITHUB_TOKEN`, `GH_TOKEN`, `GITHUB_TOKEN`

**Verdict:** CAUTION — Specs exist but need correction before machine can run.

---

## Remediation Plan

### Required Before Phase 1 Can Run

| # | Issue | Severity | Fix |
|---|-------|----------|-----|
| 1 | 7 pyright errors from Phase 0 | HIGH | Fix type errors in completion.py, provider.py, streaming.py |
| 2 | Wrong env var in draft specs | MEDIUM | Change `COPILOT_AGENT_TOKEN` → `GITHUB_TOKEN` in F-010-F-016 specs |
| 3 | Draft specs not brainstormed | MEDIUM | Run `/brainstorm` to validate Phase 1 design |

### Specific Errors to Fix

```
1. provider.py:25 — Remove unused import `CompletionConfig`
2. completion.py:61 — Add type annotation for `tools` parameter
3. completion.py:133 — Add null guard after session creation
4. completion.py:140 — Add null guard before `send_message` call
5. provider.py:66 — Add type annotation for `tools` parameter
6. streaming.py:63 — Add type annotation for `tool_calls`
```

---

## Recommended Next Steps

1. **Fix Phase 0 pyright errors** (reviewer handling this)
2. **Run `/brainstorm`** to properly design Phase 1 with corrected SDK knowledge
3. **Correct draft specs** with proper env var names
4. **Run machine** for overnight autonomous work

---

## Phase 1 Scope (from GOLDEN_VISION_V2.md)

| Feature | Description |
|---------|-------------|
| F-010 | SDK Client Wrapper (auth, lifecycle) |
| F-011 | Loop Controller (circuit breaker, turn limits) |
| F-012 | Tool Capture Strategy (first-turn-only) |
| F-013 | Event Router (coordinates components) |
| F-014 | Real SDK Integration (wire stubs) |
| F-015 | Completion Wiring (production-ready) |
| F-016 | E2E Integration Tests |

## Auth Environment Variables (from reference README)

The provider checks these in order:
1. `COPILOT_GITHUB_TOKEN`
2. `GH_TOKEN`
3. `GITHUB_TOKEN`

Recommended: `export GITHUB_TOKEN=$(gh auth token)`
