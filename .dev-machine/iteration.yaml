# =============================================================================
# provider-github-copilot-session-iteration: Single iteration of the development machine loop
# =============================================================================
#
# Called by build.yaml in a while loop. Each invocation:
#   1. Reads STATE.yaml to orient (bash)
#   2. Dispatches a fresh working session (agent)
#   3. Runs build/type-check as a structural gate (bash)
#   4. Archives completed features + old sessions, checks health (bash)
#
# Returns structured JSON so the outer loop can update_context.
#
# =============================================================================

name: "provider-github-copilot-session-iteration"
description: "Single iteration of the provider-github-copilot development machine -- orient, work, check health."
version: "1.0.0"
tags: ["provider-github-copilot", "iteration", "working-session"]

context:
  state_file: "./STATE.yaml"
  context_file: "./CONTEXT-TRANSFER.md"
  specs_dir: "./specs"
  session_count: "0"

steps:
  # Step 1: Orient -- read STATE.yaml before dispatching the working session
  - id: "orient"
    type: "bash"
    command: |
      python3 << 'PYEOF'
      import json, yaml
      try:
          with open("./STATE.yaml") as f:
              state = yaml.safe_load(f)
          blockers = state.get("blockers", [])
          # Support factored state (completed_features list)
          completed_list = set(state.get("completed_features", []))
          features = state.get("features", {})
          ready = in_progress = 0
          for fid, fd in features.items():
              s = fd.get("status", "")
              if s == "ready":
                  deps = fd.get("depends_on", [])
                  if all(d in completed_list or features.get(d, {}).get("status") in ("completed", "done") for d in deps):
                      ready += 1
              elif s == "in-progress":
                  in_progress += 1
          print(json.dumps({
              "phase": state.get("phase", 0),
              "phase_name": state.get("phase_name", "unknown"),
              "epoch": state.get("epoch", 0),
              "next_action": state.get("next_action", ""),
              "ready_count": ready,
              "completed_count": len(completed_list),
              "status": "blocked" if blockers else "healthy"
          }))
      except Exception as e:
          print(json.dumps({"status": "blocked", "error": str(e)}))
      PYEOF
    output: "orient_state"
    parse_json: true


  # Step 1.5: Module health check -- flag oversized packages before working session
  - id: "module-health-check"
    type: "bash"
    command: |
      cd /workspace 2>/dev/null || cd /home/mowrim/projects/next-get-provider-github-copilot
      echo "=== MODULE HEALTH CHECK ==="
      THRESHOLD=400
      OVERSIZED=""
      # Scan src directory for Python files
      for pkg in src/*/; do
        name=$(basename "$pkg")
        if [ -d "$pkg" ]; then
          loc=$(find "$pkg" -name "*.py" | xargs wc -l 2>/dev/null | tail -1 | awk '{print $1}')
          if [ "$loc" -gt "$THRESHOLD" ]; then
            OVERSIZED="$OVERSIZED $name($loc)"
          fi
        fi
      done
      if [ -n "$OVERSIZED" ]; then
        echo "OVERSIZED PACKAGES:$OVERSIZED"
        echo '{"health": "warn-oversized", "oversized": "'"$OVERSIZED"'"}'
      else
        echo "All packages within size limits."
        echo '{"health": "ok"}'
      fi
    output: "health_result"
    parse_json: true
    on_error: continue

  # Step 2: Dispatch fresh working session
  - id: "working-session"
    agent: "self"
    prompt: |
      You are a WORKING SESSION of the provider-github-copilot development machine.
      You start with ZERO prior context. Your job: read state, do work, persist state, exit.

      START BY READING THESE FILES (in this order):
      1. ./STATE.yaml -- current phase, what's done, what's next
      2. ./CONTEXT-TRANSFER.md -- recent decisions and handoff context
      3. .dev-machine/working-session-instructions.md -- your detailed operating procedure

      THEN READ THE RELEVANT SPECS:
      4. ./debates/GOLDEN_VISION_V2.md -- the overall architecture (always read this)
      5. Module specs for whatever module you're working on (check ./STATE.yaml module_specs)
      6. Feature specs marked "ready" in ./STATE.yaml features section

      YOUR MISSION THIS SESSION:
      - Determine what work is ready from ./STATE.yaml (next_action + ready features)
      - Pick the next batch of ready work (3 features or the current next_action)
      - For each piece of work:
        * Update ./STATE.yaml to mark it in-progress with timestamp
        * Write failing tests first (TDD)
        * Implement minimal code to pass tests
        * Run: uv run pytest tests/ -v
        * Run: uv run ruff check src/ && uv run pyright src/
        * BOTH must pass before proceeding.
        * Spawn a fresh sub-agent for antagonistic review (give it the spec + your diff)
        * Fix any review issues found
        * Mark implementation complete in STATE.yaml (status: "implemented")
        * Commits will be handled by post-session cleanup (git may not be available in container)
      - Update ./CONTEXT-TRANSFER.md with decisions made and work completed

      ## Important: Files are Factored (Do NOT Read Archives)

      STATE.yaml and CONTEXT-TRANSFER.md are kept slim (~300 lines each).
      - STATE.yaml: only active features. Completed features archived in FEATURE-ARCHIVE.yaml.
      - CONTEXT-TRANSFER.md: only last 5 sessions. Older sessions archived in SESSION-ARCHIVE.md.
      - Do NOT read FEATURE-ARCHIVE.yaml or SESSION-ARCHIVE.md -- they are append-only archives.
      - The post-session step auto-archives completed features and old sessions.

      ## Error Recovery Hierarchy (BEFORE adding blockers)

      1. **Self-fix attempt** -- Try to resolve the issue yourself (2-3 attempts max)
      2. **Expert agent consultation** -- Delegate ANALYSIS (not execution) to specialists:
         - foundation:bug-hunter for debugging
         - amplifier:amplifier-expert for Amplifier questions
         - python-dev:code-intel for Python code understanding
         - foundation:zen-architect for design decisions
      3. **Alternative approach** -- If same operation fails twice, try different method
      4. **Blocker (LAST RESORT)** -- Only add blocker after recovery attempts fail

      ## Critical Execution Rules

      - EXECUTE git commands DIRECTLY in your session (never delegate git to sub-agents)
      - DELEGATE only for ANALYSIS/REVIEW tasks (sub-agents have no bash access)
      - LSP ONLY on Python files (*.py) -- skip for YAML/Markdown/JSON
      - If LSP fails, fall back to grep/read_file

      IF BLOCKED: add blocker to ./STATE.yaml blockers list, update ./CONTEXT-TRANSFER.md, stop.
      IF DEGRADATION DETECTED (repeating yourself, losing coherence): commit work, update state, stop.

      You are one session in a long chain. Persist everything to files.
      What you don't write down is lost forever.

      ## SAFETY CONSTRAINTS (MANDATORY -- NEVER VIOLATE)
      - Your working directory is /workspace (or /home/mowrim/projects/next-get-provider-github-copilot if on host). Do NOT read, write, or execute commands on files outside this directory.
      - FORBIDDEN commands: rm -rf, find -delete, any command targeting paths outside your working directory
      - Do NOT cd to .., ~, /, or any path outside /home/mowrim/projects/next-get-provider-github-copilot
      - Do NOT access: ~/.ssh/, ~/.amplifier/keys.env, .env files, *.pem, *.key, or any credential files
      - Do NOT run: git push, git remote add, or any command that sends code to a remote repository
      - Do NOT start background processes with &, nohup, or disown unless the recipe infrastructure started them
      - Do NOT install global packages (pip install without venv, npm install -g)
      - If you need something outside these boundaries, add a blocker to ./STATE.yaml and STOP.
      - VIOLATION OF THESE CONSTRAINTS MAY CAUSE DATA LOSS ACROSS THE ENTIRE DEVELOPMENT ENVIRONMENT.
    output: "session_result"
    timeout: 3600

  # Step 3: Structural build gate -- catches errors the agent may have missed
  - id: "build-check"
    type: "bash"
    command: |
      cd /workspace 2>/dev/null || cd /home/mowrim/projects/next-get-provider-github-copilot
      echo "=== POST-SESSION BUILD CHECK ==="
      if uv run ruff check src/ && uv run pyright src/ 2>&1; then
        echo '{"build_status": "clean"}'
      else
        echo "BUILD FAILED -- errors detected after working session"
        echo "Adding blocker to ./STATE.yaml..."
        python3 -c "import yaml,datetime;f=open('./STATE.yaml');s=yaml.safe_load(f);f.close();b=s.get('blockers') or [];b.append({'description':'Build failed after working session','since':datetime.datetime.now().isoformat(),'severity':'high'});s['blockers']=b;f=open('./STATE.yaml','w');yaml.dump(s,f,default_flow_style=False,sort_keys=False);f.close()"
        echo '{"build_status": "failed"}'
      fi
    output: "build_result"
    timeout: 300

  # Step 4: Post-session -- archive completed features + old sessions, check health
  - id: "post-session"
    type: "bash"
    command: |
      python3 << 'PYEOF'
      import json, yaml, re, os
      from datetime import datetime, timezone

      try:
          with open("./STATE.yaml") as f:
              state = yaml.safe_load(f)

          session_num = int("{{session_count}}") + 1

          # --- Archive completed features (if using completed_features list pattern) ---
          features = state.get("features", {})
          completed_list = state.get("completed_features", [])
          if isinstance(completed_list, list):
              newly_completed = {fid: fd for fid, fd in features.items() if fd.get("status") in ("completed", "done")}
              if newly_completed:
                  archive_path = os.path.join(os.path.dirname("./STATE.yaml") or ".", "FEATURE-ARCHIVE.yaml")
                  try:
                      with open(archive_path) as f:
                          archive = yaml.safe_load(f) or {}
                  except FileNotFoundError:
                      archive = {"description": "Completed feature archive. Append-only.", "features": {}}
                  arch_feats = archive.get("features", {})
                  for fid, fd in newly_completed.items():
                      if fid not in completed_list:
                          completed_list.append(fid)
                      arch_feats[fid] = fd
                      del features[fid]
                  archive["features"] = arch_feats
                  state["completed_features"] = completed_list
                  state["features"] = features
                  with open(archive_path, "w") as f:
                      yaml.dump(archive, f, default_flow_style=False, sort_keys=False, width=120)

          # --- Archive old session summaries from context-transfer (keep last 5) ---
          KEEP_SESSIONS = 5
          ctx_path = "./CONTEXT-TRANSFER.md"
          session_archive = os.path.join(os.path.dirname(ctx_path) or ".", "SESSION-ARCHIVE.md")
          if os.path.exists(ctx_path):
              with open(ctx_path) as f:
                  ctx = f.read()
              sess = list(re.finditer(r'^### Session \d+ Summary', ctx, re.MULTILINE))
              if len(sess) > KEEP_SESSIONS:
                  header = ctx[:sess[0].start()]
                  keep_from = sess[-KEEP_SESSIONS]
                  to_archive = ctx[sess[0].start():keep_from.start()]
                  keep = ctx[keep_from.start():]
                  if to_archive.strip():
                      existing = ""
                      if os.path.exists(session_archive):
                          with open(session_archive) as f:
                              existing = f.read()
                      if not existing:
                          existing = "# Session Archive\n\n> Archived session summaries. Append-only.\n\n"
                      if to_archive[:50] not in existing:
                          with open(session_archive, "a") as f:
                              f.write(to_archive)
                  with open(ctx_path, "w") as f:
                      f.write(header)
                      f.write(f"> **Note**: Older sessions archived in SESSION-ARCHIVE.md.\n")
                      f.write(f"> Only the last {KEEP_SESSIONS} sessions kept here.\n\n")
                      f.write(keep)

          # --- Update epoch and check health ---
          state["epoch"] = state.get("epoch", 0) + 1
          state["last_session"] = datetime.now(timezone.utc).isoformat()
          blockers = state.get("blockers", [])

          with open("./STATE.yaml", "w") as f:
              yaml.dump(state, f, default_flow_style=False, sort_keys=False, width=120)

          remaining = sum(1 for f in features.values() if f.get("status") not in ("completed", "done"))
          status = "blocked" if blockers else ("complete" if remaining == 0 else "healthy")

          print(json.dumps({
              "status": status,
              "session_count": str(session_num),
              "next_action": state.get("next_action", ""),
              "total_features": state.get("meta", {}).get("total_features_completed", 0)
          }))
      except Exception as e:
          print(json.dumps({"status": "blocked", "session_count": "{{session_count}}", "error": str(e)}))
      PYEOF

      # --- Git commit completed work ---
      echo "Checking for uncommitted work..."
      if [ -n "$(git status --porcelain)" ]; then
        echo "Staging and committing changes..."
        git add -A
        COMMIT_MSG="feat: autonomous implementation progress - Session $(date -u +%Y-%m-%dT%H:%M:%SZ)"
        git commit -m "$COMMIT_MSG" || echo "Commit failed (maybe nothing to commit)"
        
        # Push if we have a remote
        if git remote | grep -q origin; then
          echo "Pushing to origin..."
          git push origin main || echo "Push failed - will retry next iteration"
        fi
      else
        echo "No changes to commit"
      fi

      # --- Empty session cleanup ---
      SESSIONS_DIR="$HOME/.amplifier/sessions"
      if [ -d "$SESSIONS_DIR" ]; then
        CLEANED=0
        for session_dir in "$SESSIONS_DIR"/*/; do
          [ -d "$session_dir" ] || continue
          events_file="$session_dir/events.jsonl"
          if [ ! -f "$events_file" ] || [ ! -s "$events_file" ]; then
            rm -rf "$session_dir"
            CLEANED=$((CLEANED + 1))
            continue
          fi
          line_count=$(wc -l < "$events_file" 2>/dev/null || echo "0")
          if [ "$line_count" -lt 3 ]; then
            rm -rf "$session_dir"
            CLEANED=$((CLEANED + 1))
          fi
        done
        if [ "$CLEANED" -gt 0 ]; then
          echo "Cleaned up $CLEANED empty/near-empty session directories"
        fi
      fi
    output: "iteration_result"
    parse_json: true
